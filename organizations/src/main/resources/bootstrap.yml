spring:
  application:
    name: organizations
  profiles:
    active:
      default
  cloud:
    config:
      uri: http://configserver:8888

logging:
  level:
    org:
      springframework:
        web: DEBUG
    com:
      thoughtmechanix: DEBUG