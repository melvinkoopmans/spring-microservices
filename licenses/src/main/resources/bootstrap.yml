spring:
  application:
    name: licenses
  profiles:
    active:
      default
  cloud:
    config:
      uri: http://localhost:8888

logging:
  level:
    org:
      springframework:
        web:
          client:
            RestTemplate: DEBUG