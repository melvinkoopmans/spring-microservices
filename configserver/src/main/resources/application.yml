server:
  port: 8888
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: git@github.com:melvinkoopmans/spring-microservices.git
          search-paths: config
