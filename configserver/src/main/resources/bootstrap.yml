logging:
  level:
    org:
      springframework:
        cloud: DEBUG

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/melvinkoopmans/spring-microservices
          search-paths: config
          default-label: main
